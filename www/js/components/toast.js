export class Toast{constructor(){this.container=null;this.init();}init(){this.container=document.querySelector('.toast-container');if(!this.container){this.container=document.createElement('div');this.container.className='toast-container';document.body.appendChild(this.container);}}show(message,type='info',duration=3500){if(!this.container){this.init();}const toastElement=document.createElement('div');toastElement.className=`toast toast--${type}`;let icon='ℹ️';if(type==='success')icon='✅';if(type==='error')icon='🚨';toastElement.innerHTML=`<span class="toast__icon">${icon}</span><span class="toast__message">${message}</span>`;this.container.appendChild(toastElement);const dismissToast=()=>{toastElement.classList.add('toast--remove');toastElement.addEventListener('animationend',()=>{toastElement.remove();});};const timeoutId=setTimeout(dismissToast,duration);toastElement.addEventListener('click',()=>{clearTimeout(timeoutId);dismissToast();});}}export const toast=new Toast();export default toast;