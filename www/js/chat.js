import{getSupabase,isSupabaseConfigured}from './supabase.js';import{getCurrentUser}from './auth.js';import{showToast}from './utils.js';class LiveChat{constructor(){this.currentUser=null;this.messages=[];this.isOpen=false;this.channel=null;this.supabase=null;}async init(){this.loadStylesheet();if(!isSupabaseConfigured){console.warn("LiveChat:Supabase no está configurado. Deshabilitando chat.");return;}const client=await getSupabase();if(!client){console.warn("LiveChat:No se pudo inicializar el cliente de Supabase.");return;}this.supabase=client;try{this.currentUser=await getCurrentUser();}catch(e){console.error("LiveChat:Error al obtener usuario actual",e);}this.renderUI();this.setupListeners();this.loadMessages();this.subscribeToRealtime();}loadStylesheet(){if(document.getElementById('cineverse-chat-css'))return;const link=document.createElement('link');link.id='cineverse-chat-css';link.rel='stylesheet';link.href='css/chat.css';document.head.appendChild(link);}renderUI(){const bubble=document.createElement('div');bubble.id='chat-bubble';bubble.className='chat-bubble';bubble.innerHTML=`<div class="chat-bubble-inner"><svg viewBox="0 0 24 24"><path d="M20 2H4c-1.1 0-1.99.9-1.99 2L2 22l4-4h14c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zM6 9h12v2H6V9zm8 5H6v-2h8v2zm4-6H6V6h12v2z"/></svg><span id="chat-badge" class="chat-badge">!</span></div>`;const panel=document.createElement('div');panel.id='chat-panel';panel.className='chat-panel';const isPremium=this.currentUser?.profile?.is_premium;panel.innerHTML=`<!--Cabecera del chat--><div class="chat-header"><div class="chat-header-title"><div class="chat-header-row"><span class="chat-status-dot"></span><h3>Chat en Vivo</h3></div><span class="chat-header-subtitle">Canal Público Global</span></div><button id="chat-close-btn" class="chat-close-btn">✕</button></div><!--Lista de mensajes--><div id="chat-messages-container" class="chat-messages-container"><p style="text-align:center;color:var(--text-muted);font-size:0.85rem;margin-top:2rem;">Cargando mensajes del canal...</p></div><!--Footer/Input del chat--><div class="chat-footer">${this.getChatInputHTML(isPremium)}</div>`;document.body.appendChild(bubble);document.body.appendChild(panel);}getChatInputHTML(isPremium){if(!this.currentUser){return `<div style="text-align:center;padding:0.5rem 0;"><p style="color:var(--text-muted);font-size:0.82rem;margin-bottom:0.75rem;">Inicia sesión para participar en el chat.</p><a href="login.html" class="btn btn--primary" style="display:block;font-size:0.85rem;padding:0.5rem 1rem;text-decoration:none;text-align:center;font-weight:700;border-radius:var(--radius-sm);">Iniciar Sesión</a></div>`;}if(!isPremium){return `<div style="display:flex;flex-direction:column;gap:0.75rem;"><div class="chat-input-form"><input type="text" disabled placeholder="Solo lectura para usuarios Free..." class="chat-input-text"><button disabled class="chat-send-btn"><svg viewBox="0 0 24 24"><path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/></svg></button></div><div class="chat-premium-promo"><span class="chat-promo-badge">👑 Función Exclusiva de Premium</span><p class="chat-promo-text">Los usuarios Premium pueden escribir en tiempo real. ¡Pásate a Premium para chatear!</p><a href="perfil.html?tab=premium" class="chat-promo-link">Activar Premium Aquí</a></div></div>`;}return `<form id="chat-input-form" class="chat-input-form"><input type="text" id="chat-message-text" placeholder="Escribe un mensaje(máx 150 car)..." maxlength="150" required class="chat-input-text"><button type="submit" id="chat-send-btn" class="chat-send-btn"><svg viewBox="0 0 24 24"><path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/></svg></button></form>`;}setupListeners(){const bubble=document.getElementById('chat-bubble');const panel=document.getElementById('chat-panel');const closeBtn=document.getElementById('chat-close-btn');const toggleChat=()=>{this.isOpen=!this.isOpen;if(this.isOpen){panel.classList.add('active');bubble.style.transform='scale(0.8)rotate(90deg)';bubble.style.opacity='0.5';document.getElementById('chat-badge').style.display='none';this.scrollToBottom();}else{panel.classList.remove('active');bubble.style.transform='';bubble.style.opacity='';bubble.style.boxShadow='';}};if(bubble)bubble.addEventListener('click',toggleChat);if(closeBtn)closeBtn.addEventListener('click',toggleChat);const form=document.getElementById('chat-input-form');if(form){form.addEventListener('submit',async(e)=>{e.preventDefault();const input=document.getElementById('chat-message-text');const text=input.value.trim();if(!text)return;input.value='';await this.sendMessage(text);});}const container=document.getElementById('chat-messages-container');if(container){container.addEventListener('click',async(e)=>{const btn=e.target.closest('[data-delete-msg-id]');if(!btn)return;const msgId=btn.getAttribute('data-delete-msg-id');await this.deleteMessage(msgId,btn);});}}async loadMessages(){const container=document.getElementById('chat-messages-container');if(!container||!this.supabase)return;try{const{data,error}=await this.supabase .from('chat_messages').select('*,profiles(username,display_name,avatar_url,is_premium)').order('created_at',{ascending:false}).limit(40);if(error)throw error;this.messages=(data||[]).reverse();this.renderMessages();this.scrollToBottom();}catch(e){console.error("LiveChat:Error al cargar mensajes",e);container.innerHTML='<p style="text-align:center;color:var(--accent-red);font-size:0.8rem;padding:1rem;">No se pudieron cargar los mensajes.</p>';}}renderMessages(){const container=document.getElementById('chat-messages-container');if(!container)return;if(this.messages.length===0){container.innerHTML='<p style="text-align:center;color:var(--text-muted);font-size:0.82rem;padding:2rem;">¡Canal vacío!Escribe el primer mensaje.</p>';return;}container.innerHTML=this.messages.map(msg=>this.formatMessageHTML(msg)).join('');}formatMessageHTML(msg){const profile=msg.profiles||{};const authorName=profile.display_name||profile.username||'Usuario';const avatar=profile.avatar_url||`https://api.dicebear.com/7.x/bottts/svg?seed=${encodeURIComponent(authorName)}`;const isMsgPremium=!!profile.is_premium;const tempDiv=document.createElement('div');tempDiv.textContent=msg.message;const sanitizedMsg=tempDiv.innerHTML;const badge=isMsgPremium?`<span class="chat-badge-premium">👑 PREMIUM</span>`:'';const timestamp=new Date(msg.created_at).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});const isOwn=this.currentUser&&msg.user_id===this.currentUser.id;const deleteBtn=isOwn?`<button class="chat-msg-delete-btn" data-delete-msg-id="${msg.id}" title="Borrar mensaje"><svg style="width:13px;height:13px;fill:currentColor;" viewBox="0 0 24 24"><path d="M6 19c0 1.1.9 2 2 2h8c1.1 0 2-.9 2-2V7H6v12zM19 4h-3.5l-1-1h-5l-1 1H5v2h14V4z"/></svg></button>`:'';return `<div class="chat-msg ${isOwn?'chat-msg--own':''}" data-msg-id="${msg.id}"><img class="chat-avatar ${isMsgPremium?'premium':''}" src="${avatar}" alt="${authorName}"><div class="chat-msg-body"><div class="chat-msg-header"><div class="chat-msg-author-group"><span class="chat-msg-author ${isMsgPremium?'premium':'free'}">${authorName}</span>${badge}</div><div class="chat-msg-meta"><span class="chat-msg-time">${timestamp}</span>${deleteBtn}</div></div><div class="chat-msg-bubble ${isMsgPremium?'premium-bubble':''}">${sanitizedMsg}</div></div></div>`;}async sendMessage(text){if(!this.currentUser)return;try{const{error}=await this.supabase .from('chat_messages').insert({user_id:this.currentUser.id,message:text});if(error){if(error.code==='42501'||error.message.includes('permission denied')){showToast("Solo los miembros Premium pueden chatear.","error");}else{throw error;}}}catch(e){console.error("LiveChat:Error al enviar mensaje",e);showToast("No se pudo enviar el mensaje.","error");}}async deleteMessage(msgId,btnEl){if(!this.currentUser)return;const originalText=btnEl.innerHTML;btnEl.innerHTML='⏳';btnEl.disabled=true;try{const{error}=await this.supabase .from('chat_messages').delete().eq('id',msgId).eq('user_id',this.currentUser.id);if(error)throw error;const msgEl=document.querySelector(`.chat-msg[data-msg-id="${msgId}"]`);if(msgEl){msgEl.style.transition='all 0.3s cubic-bezier(0.4,0,0.2,1)';msgEl.style.opacity='0';msgEl.style.transform='translateX(20px)';setTimeout(()=>msgEl.remove(),300);}this.messages=this.messages.filter(m=>m.id!==msgId);}catch(e){console.error("LiveChat:Error al borrar mensaje",e);btnEl.innerHTML=originalText;btnEl.disabled=false;showToast("No se pudo borrar el mensaje.","error");}}subscribeToRealtime(){if(!this.supabase){console.warn('LiveChat:supabase no disponible,cancelando suscripción en tiempo real.');return;}this.channel=this.supabase .channel('chat-realtime-v3').on('postgres_changes',{event:'INSERT',schema:'public',table:'chat_messages'},async(payload)=>{const newMsg=payload.new;const{data:profile}=await this.supabase .from('profiles').select('username,display_name,avatar_url,is_premium').eq('id',newMsg.user_id).maybeSingle();newMsg.profiles=profile||{};this.messages.push(newMsg);if(this.messages.length>50)this.messages.shift();const container=document.getElementById('chat-messages-container');if(container){if(this.messages.length===1)container.innerHTML='';const div=document.createElement('div');div.innerHTML=this.formatMessageHTML(newMsg);const msgNode=div.firstElementChild;container.appendChild(msgNode);const delBtn=msgNode?.querySelector('[data-delete-msg-id]');if(delBtn){delBtn.addEventListener('click',async()=>{await this.deleteMessage(newMsg.id,delBtn);});}if(this.isOpen){this.scrollToBottom();}else{const badge=document.getElementById('chat-badge');if(badge)badge.style.display='flex';}}}).on('postgres_changes',{event:'DELETE',schema:'public',table:'chat_messages'},(payload)=>{const deletedId=payload.old?.id;if(!deletedId)return;const msgEl=document.querySelector(`.chat-msg[data-msg-id="${deletedId}"]`);if(msgEl){msgEl.style.transition='opacity 0.3s';msgEl.style.opacity='0';setTimeout(()=>msgEl.remove(),300);}this.messages=this.messages.filter(m=>m.id!==deletedId);}).subscribe();}scrollToBottom(){const container=document.getElementById('chat-messages-container');if(container){setTimeout(()=>{container.scrollTop=container.scrollHeight;},50);}}}